# Claude Code Starter Project - Environment Variables
# Copy this file to .envrc and fill in your values
# Requires direnv: https://direnv.net/

# =============================================================================
# MCP Server Credentials
# =============================================================================

# GitHub Personal Access Token (required for GitHub MCP server)
# Create at: https://github.com/settings/tokens
# Scopes needed: repo, read:org, read:user
export GITHUB_TOKEN="your-github-personal-access-token"

# Google OAuth Credentials (optional - for Google Workspace MCP server)
# Create at: https://console.cloud.google.com/apis/credentials
export GOOGLE_OAUTH_CLIENT_ID="your-google-oauth-client-id"
export GOOGLE_OAUTH_CLIENT_SECRET="your-google-oauth-client-secret"

# =============================================================================
# AWS Settings (used by awslabs MCP servers)
# =============================================================================

# AWS Profile from ~/.aws/credentials (recommended over access keys)
export AWS_PROFILE="default"

# AWS Region for MCP server operations
export AWS_REGION="us-east-1"

# Alternative: Direct credentials (not recommended - use profiles instead)
# export AWS_ACCESS_KEY_ID="your-access-key-id"
# export AWS_SECRET_ACCESS_KEY="your-secret-access-key"

# =============================================================================
# Optional: Additional MCP Server Settings
# =============================================================================

# Spec Workflow Dashboard Port (default: 5173)
# export SPEC_WORKFLOW_PORT="5173"

# DynamoDB Local endpoint (for local development)
# export DYNAMODB_LOCAL_ENDPOINT="http://localhost:8000"
