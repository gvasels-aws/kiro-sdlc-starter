# direnv configuration for Kiro CLI SDLC Sample Project
# Install direnv: https://direnv.net/docs/installation.html
#
# Setup Instructions:
# 1. Copy this file: cp .envrc.example .envrc
# 2. Edit .envrc with your actual values
# 3. Run: direnv allow

# Python virtual environment
layout python3

# Project-specific environment variables
export PYTHONPATH="${PWD}/src:${PYTHONPATH}"

# Kiro CLI configuration
export KIRO_PROJECT_PATH="${PWD}"

# Quality gate thresholds (optional overrides)
export COVERAGE_THRESHOLD=80
export LINT_MAX_ERRORS=0

# Development settings
export PYTHONDONTWRITEBYTECODE=1
export PYTHONUNBUFFERED=1

# Log information
echo "Kiro CLI SDLC Sample Project environment activated"
echo "Python: $(python --version)"
echo "Project: ${PWD}"
